# Production build flags (committed — Vite gives this file precedence over
# .env.local in `vite build`, so a local demo/off setting never leaks into a
# deploy). Flip to 'false' to dark-ship either experiment.
VITE_FEATURE_SKY=true
VITE_FEATURE_STICKERS=true
